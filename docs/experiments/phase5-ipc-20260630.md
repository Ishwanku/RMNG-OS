# Phase 5 — rmngd IPC + integrations/dev

**Date:** 2026-06-30  
**Status:** ✅ PASSED

## Delivered

| Item | Description |
|------|-------------|
| `rmng-core::ipc` | Unix socket client (`send_intent_json`) |
| `rmng-core::response` | `HandleResponse` JSON for daemon replies |
| `rmngd` | Read intent line → dispatch → write JSON response |
| `rmng send` | CLI client for daemon |
| `rmng ask` | Prefers daemon when running |
| `integrations/dev/kernel.json` | Body-layer tool manifest |
| `scripts/install-rmng.sh` | `cargo install` rmng + rmngd |

## Verify

```bash
cd ~/dev/projects/RMNG-OS/agents
cargo test

rmngd &
rmng send -f schemas/kernel-status.intent.json
rmng status   # rmngd: running
```

## Next

1. Ollama + `rmng ask` end-to-end with daemon
2. `git.status` tool + integrations manifest
3. systemd user unit for `rmngd`
